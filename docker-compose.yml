version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: portfolio_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: portfolio
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./migrations:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Optional: Run backend in Docker (alternative to local ./gradlew run)
  # backend:
  #   build:
  #     context: ./backend
  #     dockerfile: Dockerfile
  #   container_name: portfolio_backend
  #   environment:
  #     DATABASE_URL: jdbc:postgresql://postgres:5432/portfolio
  #     SUPABASE_URL: ${SUPABASE_URL}
  #     SUPABASE_KEY: ${SUPABASE_KEY}
  #     SUPABASE_JWT_SECRET: ${SUPABASE_JWT_SECRET}
  #     PORT: 8080
  #   ports:
  #     - "8080:8080"
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   volumes:
  #     - ./backend:/app

  # Optional: Supabase local development (requires Supabase CLI)
  # Run: npx supabase start
  # This provides local Storage, Auth, and Database

volumes:
  postgres_data:
    driver: local

networks:
  default:
    name: portfolio_network
